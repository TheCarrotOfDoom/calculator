
#include <string>
#include <iostream>
#include <sstream>
#include <vector>
#include "Register.h"
#include "Operator.h"

using namespace std;

int main()
{
    string expression{};
    vector<Register> Registers{};

    cin >> expression;

    if (expression != "print" || "quit")
    {
        
    }

}